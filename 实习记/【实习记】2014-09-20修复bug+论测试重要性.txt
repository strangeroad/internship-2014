【实习记】2014-09-20修复bug+论测试重要性

bug 1
类构造函数出错，逻辑部分正确，以至于运行时仅部分时间好，部分时间有不好。
我就想代码没改，怎么就突然不行，就是应为没有完全正确。所以测试的完整行很重要。
由此感叹，测试也是很重要的，差的测试就是像昨天的我一样。

bug 2
共享内存的填充函数用到了shmat函数，却忘了shmdt解除地址映射。
现象是每次更新共享内存，都产生状态为dest，key是0的僵尸共享内存块。
使用stop才从内核删除，并且新共享内存的attach属性至少为1。

